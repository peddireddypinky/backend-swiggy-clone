# Deployment Guide

## Local Development
1. Install dependencies: npm install
2. Start MongoDB locally or use the fallback local URI.
3. Run: npm start

## Production Deployment
- Set environment variables for MONGO_URI, JWT_SECRET, and PORT.
- Deploy the Node.js app to your preferred platform.
- Ensure MongoDB Atlas or a managed MongoDB instance is reachable.
